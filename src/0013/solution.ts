export class Solution {
    /**
     * @param source:
     * @param target:
     * @return: return the index
     */
    strStr (source: string, target: string): number {
        return source.indexOf(target);
    }
}
