export class Solution {
    /**
     * @param nums: the list of numbers
     * @return: return the maximum number.
     */
    maxNum (nums: number[]): number {
        return Math.max(...nums);
    }
}
