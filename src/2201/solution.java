public class Solution {
    /**
     * @param str: represents the string passed in
     * @return: represents the element whose index
     *          is the last digit
     */
    public char getLastIndex(String str) {
        return str.charAt(str.length()-1);
        
    }
}